# G1-B.1 — EVALUATION CONTRACT & CODE FREEZE

**Estado: PASS** — código y semántica de evaluación congelados; holdouts G1
siguen cerrados. HEAD: `c1e15f2`.

## Qué se congeló

### 1. Code freeze (`g1/manifests/code-freeze.json`)

```text
git commit:        c1e15f20de97fd7e32d7b98c76ffb8554fdb40b5
git tree:          13af93ae4d95f382d50d9d40f2f1083d8a07ea97
python:            3.13.11
dependency lock:   requirements-lock.txt sha ba2f63cb…
src tree sha:      d7096206… (todos los módulos, por fichero)
freeze hashes:     G1-A initial + G1-A1 (todos los manifests)
```

Política: NO CODE / NO EXTRACTOR / NO LINKER / NO FIELD MODEL changes hasta
terminar G1-C.

### 2. Scoring contract (`g1/manifests/scoring-contract.json`)

Resuelve las tres ambigüedades detectadas antes de abrir holdouts:

**a) Denominadores de linkage.** `auto_link_coverage` usa `gold_linkable`
(label=LINK) como denominador; `review_rate` usa `n_all`. No comparten
denominador — sumar >1 es correcto por contrato. Ninguna métrica `coverage`
sin denominador declarado. Se reportan `n_all / gold_linkable / gold_no_link /
gold_review_required / pred_*` por separado.

**b) Multi-value scoring.** `EXACT / PARTIAL / SUPERSET_WITH_ERROR / DISJOINT /
MISSING / NOT_APPLICABLE` con scoring a nivel elemento. `SUPERSET_WITH_ERROR`
produce FP — no se concede precision 1.0 por un candidato correcto entre
varios. La unidad de verdad por campo se fija en el annotation contract antes
de anotar, no caso a caso tras ver predicciones.

**c) Classification.** Dos categorías: `METADATA_DETERMINED` (éxito válido pero
demuestra determinismo del contrato CNMV) y `CONTENT_DETERMINED`. Reportadas
desagregadas; sin nuevo threshold.

También congela: ExerciseTerms structural diagnostic + vista legacy para
thresholds; structured scoring por rol (sin `barrier` plano); suficiencia de
evidencia (INSUFFICIENT/DESCRIPTIVE/GATE_ELIGIBLE); safety gates sin relajar;
lifecycle (SUPPLEMENTS/MODIFIES gate-eligible, resto INSUFFICIENT_EVIDENCE);
novelty solo sobre el SRS n=100; blind truth protocol; truth corrections
v1/v2; retry policy.

## Freeze guard (before/after)

```text
tests/test_g1_freeze_guard.py + test_freeze_guard.py + test_g1b_regressions.py
22 passed — manifests G1-A/G1-A1 byte-identicos, self-hashes consistentes,
sin refs de holdout en src/
```

## Verdict

PASS. G1-C ONE-SHOT BLIND EVALUATION puede abrirse: cuatro evaluaciones
independientes (linkage 124, extraction 97, lifecycle 49, novelty 100 SRS).

Desviaciones: ninguna. No se tocó código de extractor/linker/field model;
`field-support-freeze.json` se actualizó solo en extractor_version (maintenance
del mapa de soporte, no artefacto protegido).
