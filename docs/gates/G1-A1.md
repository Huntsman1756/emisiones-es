# G1-A1 — HOLDOUT HYGIENE & NOVELTY SAMPLING

Estado: **FROZEN**. 2026-09-17. G1-B no iniciado; cero código productivo tocado.

```text
initial freeze:  a4beaf28 (G1-A, commit 8923518) — preservado íntegro
final freeze:    6924078c (g1/manifests/g1-a1/freeze.json)
verdict:         PASS tras resplit (el freeze inicial FALLABA aislamiento)
```

## Veredicto del audit sobre el freeze inicial

```text
literal duplicates            = 0          OK (ya estaba)
family leakage dev↔holdouts   = masivo     FAIL → resplit
    EH∩dev 22 | EH∩G0 23/40 | LK∩dev 64 | LK∩G0 30/74
    LC∩dev 31 | LC∩G0 19/34
cold-issuer slice             = 0/97       FAIL → corregido (25/97)
novelty representativo        = ausente    FAIL → novelty holdout creado
```

Causa raíz: el pool CCFF real son ~29 programas recurrentes; G0 ya había
tocado 23 de ellos, y los CCFF no seleccionados quedaban como hermanos en dev.

## Resplit aplicado

Unidad de aislamiento = `document_family_key` (evidencia estructural;
`PROG_<numfol>` para CCFF/FOL/FAM, `ISSUE_ADM_<reg>` para ADM; nunca por
proximidad temporal; UNKNOWN si no demostrable — 0 UNKNOWN en la práctica).

- Familias referenciadas por cualquier holdout → fuera de dev.
- Hermanos no seleccionados → quarantine (120 registros).
- Casos G0 en familias holdout → `DEV_RESTRICTED` (45/165 familias G0).
- Cold-issuer: 3 emisores puestos en cuarentena total → 25/97 docs EH (25.8%).

Resultado verificado por assert: `dev_fams ∩ holdout_fams = ∅`.

## Artefactos

| Artefacto | sha256 |
|---|---|
| g1/manifests/family-map.json | 2b598ddac315627f |
| g1/manifests/novelty-holdout.json | e0fb2815d5285fab |
| g1/manifests/annotation-contract.json | b5a578d21c610884 |
| g1-a1/development.json | cbdc890bc906aba1 |
| g1-a1/extraction-holdout.json | 7288740d74ac4b31 |
| g1-a1/linkage-holdout.json | 32ea9a3a521385f3 |
| g1-a1/lifecycle-holdout.json | 09a3f2807f6a3493 |
| g1-a1/freeze.json | 6924078c5b3808e7 |

Membership de los holdouts extraction/linkage/lifecycle **sin cambios**
(la contaminación era dev-side; la selección nunca usó salidas del extractor).

## Novelty holdout

```text
n=100, SRS seed 20260917, población elegible=291
weighting: no requerido (SRS)
gate: novelty >= 80% (sin cambios) + IC binomial exacto
independiente de dev por family isolation
```

## Diversidad de familias críticas (máximo observable)

| proxy | docs | issuers | familias |
|---|---|---|---|
| callissuer | 23 | 3 | 6 |
| put | 46 | 5 | 11 |
| day_count basis | 41 | 12 | 20 |
| fixed_rate | 24 | 5 | 8 |
| euribor | 14 | 8 | 10 |
| barrier | 38 | 2 | 6 |

`call_put` y `barrier` no alcanzan 8 emisores: concentración real del mercado
español de estructurados (BBVA GMB / SG). Documentado como límite observable;
no se sustituyeron casos por rendimiento.

## Desviaciones G1-A1

1. Holdouts retenidos íntegros; el aislamiento se logró reduciendo dev
   (565→331 docs nuevos) y restringiendo 45 familias G0 — alternativa
   (holdouts solo con familias limpias) dejaba solo 2 CCFF elegibles.
2. Cold-issuer = 25.8% superando el mínimo 20%; 3 emisores cuarentenados.
3. Diversidad issuer/familia por debajo de ≥8 en call_put/barrier/fixed —
   máximo observable del universo, documentado.
4. Lifecycle: CORRECTS/REPLACES/REDEPOSIT = INSUFFICIENT_EVIDENCE
   (preregistrado; ausencia de material no es FAIL ni PASS).
5. Novelty elegible limitado a registros con `document_url`; 100/100 con
   sha256 verificado.

**Stop**: G1-B implementation no iniciada.
